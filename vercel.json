{
    "buildCommand": "trunk build --release",
    "outputDirectory": "dist",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}